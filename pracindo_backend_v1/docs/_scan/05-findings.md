# FASE 5 — TEMUAN & RISIKO

Diurutkan dari yang paling berbahaya. **Tidak ada satu baris kode pun yang
diubah** — dokumen ini hanya melaporkan.

Ringkasan: **8 CRITICAL · 17 HIGH · 24 MEDIUM · 15 LOW** = 64 temuan.

---

## CRITICAL

| # | Lokasi | Masalah | Dampak | Saran perbaikan |
|---|---|---|---|---|
| C1 | `keuangan/views.py:10` | `PengeluaranViewSet` tidak menetapkan `permission_classes` maupun atribut `modul`, sehingga jatuh ke default DRF `IsAuthenticated` (`settings.py:159-161`). | **Setiap pengguna yang login — termasuk role `STAFF` yang baru disetujui — bisa mengeluarkan uang kas kecil, memotong saldo rekening, dan memposting jurnal `BEBAN_KAS` ke buku besar.** Seluruh pemisahan tugas AKUNTING/KEUANGAN yang dibangun di `staff_user/permissions.py:11-18` tidak berlaku untuk modul ini. | Pasang `modul = 'keuangan'` + `permission_classes = [AksesModul]` seperti `akunting/views.py:335-336`. |
| C2 | `keuangan/views.py:10` | `PUT`, `PATCH`, dan `DELETE` pada `/api/v1/keuangan/pengeluaran/{id}/` **tidak di-override**. Serializer mengizinkan tulis `kategori`, `pemohon`, `nominal`, `bukti_nota` (`keuangan/serializers.py:11-17`). | `nominal` bisa diubah **tanpa** menyentuh `MutasiKas`, `RekeningBank.saldo`, maupun jurnal. `DELETE` menghapus baris pengeluaran dan meninggalkan `MutasiKas` + jurnal yatim. Buku kas, saldo bank, dan buku besar langsung menyimpang tanpa jejak. Tidak ada signal yang menangkapnya (FASE 4 §5). | Kembalikan 405 seperti `warehouse/views.py:120-133`, atau sediakan aksi pembatalan yang menerbitkan mutasi + jurnal balik. |
| C3 | `akunting/views.py:348` | `PembayaranView.create` membangkitkan `idem_key=f'bayar:{uuid.uuid4()}'` **baru setiap request**, padahal `alokasi_pembayaran()` (`akunting/services.py:186`) dirancang idempoten lewat kunci itu. | Klik ganda, retry jaringan, atau retry proxy = **pembayaran hutang dobel**. Kas berkurang dua kali, `KartuHutang` dobel, dua jurnal `BAYAR_HUTANG`. Kolom `KartuHutang.referensi` juga tidak `unique` (`akunting/models/hutang.py:203`) sehingga DB tidak menangkapnya. | Terima `idem_key` dari klien (header `Idempotency-Key`) atau turunkan dari data bisnis; jadikan `KartuHutang.referensi` `unique`. |
| C4 | `keuangan/services.py:46` | `idempotency_key=f"pc-{pengeluaran.id}-{uuid.uuid4()}"` — sama seperti C3, komponen acak membatalkan seluruh gunanya kolom `UNIQUE`. `idem_key` itu juga diteruskan ke `posting()` (`:58`). | Retry = pengeluaran kedua + mutasi kas kedua + jurnal kedua. | Kunci deterministik, mis. `f"pc-{pengeluaran.id}"`. |
| C5 | `akunting/services.py:233-238` | `alokasi_pembayaran()` memposting `BAYAR_HUTANG` sebesar **nominal penuh**, termasuk bagian yang tidak teralokasi dan menjadi `UangMukaSuplier` (`:227-231`). Aturannya `Dr 2100 Hutang Usaha / Cr 1100 Kas` (`posting_rules.py:55-58`). | **Hutang usaha didebet lebih besar daripada hutang yang benar-benar berkurang.** Kelebihan bayar mengurangi saldo hutang di buku besar padahal secara subledger menjadi uang muka. Neraca salah sejak transaksi pertama yang melebihi tagihan. Kalau tidak ada faktur terbuka sama sekali, seluruh nominal mendebet hutang yang tidak ada. | Pecah jurnal: sebesar yang teralokasi ke `2100`, sisanya ke akun uang muka suplier (belum ada di COA `akunting/migrations/0005_seed_coa.py`). |
| C6 | `inventory/services.py:263-302` | `pakai_dari_pool()` dan `hasil_ke_pool()` menggerakkan stok POOL **tanpa menulis satu baris pun ke `MutasiKlaim`/`PosisiKlaim`**. | Invariant utama SZA — `SUM(PosisiKlaim.nilai_bersih) = nilai ekuivalen sisa POOL` (`inventory/services.py:18`, `inventory/models.py:14`) — **rusak permanen setiap kali satu sesi produksi selesai**, karena nilai ekuivalen bahan yang dipakai ≠ nilai ekuivalen produk jadi yang masuk. Angka "siapa berhutang ke siapa" antar PT/CV/Agus/Marsini jadi tidak bisa dipercaya, dan `verifikasi_pool_bersih()` akan selalu melaporkan selisih tanpa ada mekanisme koreksi. | Tentukan aturan konversi klaim saat produksi (mis. rasio nilai ekuivalen input→output ditulis sebagai `MutasiKlaim` jenis `KOREKSI` per entitas menurut porsi setoran), lalu panggil dari `mulai_sesi`/`selesaikan_sesi`. |
| C7 | `keuangan/services.py:29,34` | `nominal` dibaca mentah dari `request.data` (`keuangan/views.py:30`) dan **tidak pernah dikonversi ke `Decimal`**, lalu dibandingkan dan dikurangkan terhadap `rekening.saldo` (Decimal). | Multipart form (jalur normal karena ada `bukti_nota`) mengirim `str` → `TypeError: '<' not supported between 'Decimal' and 'str'` di `:29`. JSON `float` → `TypeError` di `:34` (`Decimal − float` tidak didukung). Hanya JSON `int` yang bekerja. Ditelan `except Exception` (`keuangan/views.py:35`) dan disajikan sebagai HTTP 400 dengan pesan Python mentah. **Baris `PengeluaranKas` sudah dibuat di `:12` sebelum galat terjadi**, dan `bukti_nota` sudah ditulis ke storage — rollback membatalkan barisnya tapi meninggalkan berkas yatim. | Validasi payload dengan serializer (`DecimalField(max_digits=18, decimal_places=2, min_value=0.01)`) sebelum memanggil service. |
| C8 | `pracindo_erp/urls.py` + `Dockerfile:22` + tidak ada `.dockerignore` | `Dockerfile` menyalin `COPY . .` (`Dockerfile:18`) tanpa `.dockerignore`, sehingga `.env` (berisi `SECRET_KEY`, `DB_PASSWORD`, dan `DEBUG=True`) serta direktori `logs/` ikut masuk ke image. `CMD` memakai `python manage.py runserver` (`Dockerfile:22`). | Rahasia produksi terbungkus di layer image yang bisa di-`docker save`/di-pull siapa pun yang punya akses registry. `DEBUG=True` mematikan **seluruh** blok keamanan `settings.py:258-267` (SSL redirect, HSTS, cookie secure, nosniff, X-Frame-Options) dan membocorkan traceback berisi query & setting. `runserver` adalah server pengembangan — single-threaded, tidak untuk produksi. | Tambah `.dockerignore` (`.env`, `logs/`, `.git`, `media/`), pindah ke `gunicorn`, dan pastikan `DEBUG` dari environment orkestrator, bukan dari berkas yang ikut ter-COPY. |

---

## HIGH

| # | Lokasi | Masalah | Dampak | Saran perbaikan |
|---|---|---|---|---|
| H1 | `work_order/views.py:14` | `permission_classes = [IsAuthenticated]`; tidak ada `modul`, tidak ada cek kepemilikan pada `update`/`destroy`. | Setiap pengguna login bisa `PUT`/`PATCH`/`DELETE` work order milik siapa pun. `DELETE` menghapus permanen beserta seluruh `WorkOrderPenugasan` (CASCADE, `work_order/models.py:54`). | `modul = 'work_order'` + `AksesModul`, plus cek pembuat/penerima tugas untuk write. |
| H2 | `work_order/views.py:25` dan `:51` | `getattr(request.user, 'profil_staff_id', None)` — atribut `profil_staff_id` **tidak ada** pada model `Profil` (`staff_user/models.py:104-239`). Nilainya selalu `None`. | `GET /api/v1/work-order/mading/` **selalu mengembalikan `[]`** — papan tugas pribadi tidak pernah menampilkan apa pun. `POST /api/v1/work-order/{id}/approve/` **selalu 403** karena `wo.penugasan.filter(staff_id=None)` tidak pernah cocok. Seluruh modul work order tidak berfungsi. | `WorkOrderPenugasan.staff` menunjuk `staff_user.Profil` langsung (`models.py:56`), jadi yang benar adalah `request.user.id`. |
| H3 | `akunting/services.py:589-632` | Saat menerbitkan faktur, GRNI dibersihkan sebesar `nilai_wajar = nilai_terima − potongan`, sedangkan yang dikreditkan saat penerimaan adalah `nilai_terima` penuh (`warehouse/services.py:215-224`). | **Saldo GRNI (akun 2190) menggantung sebesar potongan klaim untuk selamanya.** Tidak ada jurnal mana pun yang membebankan potongan klaim ke akun beban/piutang klaim. Laporan "barang diterima belum difaktur" jadi tidak bisa dipercaya, dan angkanya bertambah setiap ada klaim POTONG_TAGIHAN. | Terbitkan jurnal ketiga saat faktur: `Dr 2190 GRNI / Cr <akun klaim suplier>` sebesar `potongan`. |
| H4 | `inventory/services.py:401-444` | `sesuaikan_stok()` (opname) mengubah `Stok.qty` dan `SaldoEntitas.nilai` **tanpa memposting jurnal apa pun**. | Nilai persediaan di neraca (akun 1300) langsung menyimpang dari `SUM(SaldoEntitas.nilai)` setiap kali opname dijalankan. Tidak ada rekonsiliasi yang menutupnya. Parameter `nilai_penyesuaian` (`:441`) menerima angka bebas dari payload — Supervisor bisa merevaluasi persediaan tanpa jejak akuntansi. | Panggil `akunting.services.posting()` dengan aturan baru `OPNAME_LEBIH`/`OPNAME_KURANG` (Dr/Cr Persediaan vs akun selisih persediaan). |
| H5 | `akunting/services.py:186-239` | `alokasi_pembayaran()` tidak menyentuh `keuangan.MutasiKas` maupun `RekeningBank.saldo` sama sekali. | Kas berkurang di buku besar (`Cr 1100`) tapi **tidak di buku bank**. Saldo `RekeningBank` hanya bergerak lewat kas kecil (`keuangan/services.py:34`). Rekonsiliasi bank tidak akan pernah cocok. `RencanaBayar` yang dirancang untuk menjembatani ini (`keuangan/models.py:128`) tidak dipakai siapa pun. | Pembayaran suplier harus menerbitkan `MutasiKas` pada rekening yang dipilih, di dalam transaksi yang sama. |
| H6 | `master/utils.py:9-24` | `generate_kode_urut()` membaca `.order_by(field).last()` **tanpa lock dan tanpa transaksi**, lalu menambah 1. | (a) Dua request bersamaan menghasilkan kode identik → `IntegrityError` 500. (b) `order_by` **leksikografis**: setelah `BP-9999` datang `BP-10000`, dan `'BP-10000' < 'BP-9999'` sebagai string → `.last()` selamanya mengembalikan `BP-9999` → **kode duplikat permanen**. (c) `except (IndexError, ValueError): n = 1` (`:19-20`) — satu kode yang diketik manual dengan format lain membuat generator mengulang dari `0001`. Dipakai `Produk`, `Suplier`, `Pelanggan`, `Kategori`. | Pakai pola `CounterDokumen.berikutnya()` (`core/models.py:181`) yang sudah benar: `select_for_update` + kolom counter numerik. |
| H7 | `work_order/models.py:40-50` | `WorkOrder.save()` menghasilkan nomor dengan `filter(nomor__startswith=prefix).order_by('nomor').last()` — masalah identik H6, ditambah `nomor` `unique=True`. | Balapan → `IntegrityError` 500. Setelah `WO/2026/08/999`, urutan leksikografis membuat `.last()` mandek → semua WO berikutnya di bulan itu bentrok. | idem H6. |
| H8 | `work_order/serializers.py:36-43` | `WorkOrderSerializer.create()` menulis `WorkOrder` lalu `N × WorkOrderPenugasan` **tanpa `transaction.atomic`**. `staff_ids` hanya `ListField(IntegerField)` (`:23`) tanpa validasi keberadaan. | `staff_id` yang tidak ada → `IntegrityError` **setelah** `WorkOrder` tersimpan → work order yatim tanpa penugasan, dan nomor terpakai. | Bungkus `@transaction.atomic` dan pakai `PrimaryKeyRelatedField(queryset=Profil.objects.aktif(), many=True)`. |
| H9 | `warehouse/serializers.py:196` | `SelesaikanSelisihSerializer.resolusi = CharField(max_length=8)` — bukan `ChoiceField(Resolusi.choices)`. | Nilai salah ketik lolos validasi, gagal cocok dengan `Resolusi.POTONG_TAGIHAN` di `warehouse/services.py:357`, jatuh ke cabang `else` → **`nilai_klaim` dipaksa `0`** dan laporan ditutup sebagai `DISELESAIKAN` **tanpa klaim, diam-diam**. Potongan tagihan ke suplier hilang tanpa pesan galat. | `serializers.ChoiceField(choices=Resolusi.choices)`. |
| H10 | `warehouse/serializers.py:189` | `LaporanManualSerializer.jenis = CharField(max_length=14)` — bukan `ChoiceField(JenisSelisih.choices)`. | String sembarang masuk ke kolom ber-`choices`; `get_jenis_display()` mengembalikan nilai mentah, filter `?jenis=` tidak menemukannya. | `ChoiceField(choices=JenisSelisih.choices)`. |
| H11 | `settings.py:75-96` + `pracindo_erp/urls.py:7-27` | App `audit` **tidak terdaftar** di `INSTALLED_APPS` dan `audit.urls` tidak di-`include`, padahal implementasinya lengkap (model 117 baris, service 83 baris, viewset, serializer, admin, migrasi). `audit.services.catat()` tidak dipanggil satu berkas pun. | **Tidak ada jejak sama sekali** untuk perubahan status yang tidak menghasilkan jurnal: siapa membatalkan PO, siapa menutup periode, siapa mengubah role, siapa menyelesaikan laporan selisih. Persis skenario yang docstring `audit/models.py:13-18` sebut sebagai "yang paling sering ditanyakan saat ada masalah". Tabel `audit_jejak_aktivitas` tidak akan pernah dibuat. | Tambahkan `'audit'` ke `LOCAL_APPS`, `path('api/v1/audit/', include('audit.urls'))`, lalu panggil `catat()` dari 12 titik transisi status di `services.py` masing-masing app. |
| H12 | `staff_user/views.py:147,161` | `POST /api/v1/auth/profil/` memakai `ProfilSerializer` (`serializers.py:27`) yang **tidak punya field `password`**, dan `is_active` `read_only` sehingga memakai default `AbstractUser` = **`True`**. | Admin bisa membuat akun yang **langsung aktif**, melewati seluruh alur pendaftaran→persetujuan yang dibangun di `services.daftar()`/`aktifkan()`. Password-nya "unusable" sehingga tidak bisa login lewat password — tapi akunnya nyata, muncul di daftar, dan bisa diberi role lewat `ubah-role`. | Blokir `create` (405) dan arahkan ke alur `daftar` + `aktifkan`. |
| H13 | `settings.py:150-172` | **Tidak ada throttling sama sekali** (`grep throttle` → 0). `POST /auth/login/` dan `POST /auth/daftar/` `AllowAny` (`staff_user/views.py:33,61`). | Brute force password tanpa hambatan. Setiap percobaan login gagal **menulis satu baris `RiwayatAkses`** (`staff_user/services.py:206`) → penyerang bisa menggelembungkan tabel audit dari anonim. Pendaftaran massal juga tanpa hambatan. | `DEFAULT_THROTTLE_CLASSES` + `AnonRateThrottle` (mis. `5/min` untuk login), atau throttle scope khusus. |
| H14 | `akunting/views.py:276-299` | `POST /faktur/dari-penerimaan/{id}/` **tidak memanggil `bisa_akses_entitas()`**, berbeda dari `PurchaseOrderViewSet.create` (`:138`) dan `PembayaranView.create` (`:342`) yang punya. | Pengguna dengan role AKUNTING dan `entitas_diizinkan` terbatas tetap bisa **menerbitkan faktur dan memposting jurnal untuk entitas yang bukan haknya**. Pembatasan entitas hanya berlaku pada pembacaan (`filter_entitas`), bukan penulisan. | Tambahkan cek yang sama sebelum memanggil service. Idem untuk `warehouse/views.py:87`, `inventory/views.py:161,183`, `produksi/views.py:45`. |
| H15 | `akunting/views.py:217-219` | Aksi `ringkasan` memanggil `services.ringkasan_po(pk)` **tanpa `filter_entitas`**, berbeda dari `get_queryset()` di viewset yang sama. | Siapa pun dengan modul akunting bisa membaca rincian PO lintas entitas (nomor, suplier, harga, amount) hanya dengan menebak id. `UangMukaViewSet` (`:359`) juga tanpa filter entitas. | Ambil objek lewat `self.get_object()` yang sudah tersaring, bukan `pk` mentah. |
| H16 | `akunting/models/hutang.py:203` | `KartuHutang.referensi` tidak `unique` dan tidak berindeks, padahal `post_pembayaran()` memakainya sebagai penjaga idempotensi (`akunting/services.py:160`). | Cek "sudah ada?" hanya `SELECT` tanpa penjaga DB — dua transaksi paralel dengan `idem_key` sama lolos berdua. Ditambah C3, jalur pembayaran tidak punya perlindungan berlapis sama sekali. Query `filter(referensi=…)` juga full scan. | `unique=True` pada `referensi` (atau kolom `idempotency_key` terpisah seperti `MutasiStok`/`MutasiKas`). |
| H17 | `akunting/services.py:110`, `:136` | `JurnalDetail.objects.bulk_create()` **melewati `clean()`** (`akunting/models/jurnal.py:152`) yang menolak posting ke akun header (`boleh_diposting=False`). | Tidak ada penjaga apa pun terhadap posting ke akun header. Trigger DB hanya memeriksa keseimbangan, bukan jenis akun. Satu entri salah di `posting_rules.py` akan lolos diam-diam dan merusak struktur laporan. | Tambah `CheckConstraint` lintas-tabel lewat trigger kedua, atau validasi kode akun terhadap `Akun.objects.bisa_diposting()` di `posting()` sebelum `bulk_create`. |

---

## MEDIUM

| # | Lokasi | Masalah | Dampak | Saran perbaikan |
|---|---|---|---|---|
| M1 | `inventory/services.py:263-302` | `pakai_dari_pool()`/`hasil_ke_pool()` **tidak memanggil `pastikan_periode_terbuka()`**, berbeda dari `terima_raw`, `setor_ke_pool`, `klaim_hasil`. | Seluruh alur produksi bisa menggerakkan stok ke periode yang sudah dikunci Supervisor. | Tambahkan pemeriksaan; entitas tidak relevan di POOL, jadi periksa per entitas dalam grup atau tambahkan pemeriksaan global. |
| M2 | `keuangan/services.py:55` | Jurnal kas kecil dipaksa `tanggal=timezone.localdate()`, bukan tanggal transaksi. | Pengeluaran yang diinput mundur tetap diposting ke hari ini. Efeknya, `pastikan_periode_terbuka()` yang dipanggil `posting()` **selalu lolos** — pemeriksaan periode tidak berfungsi untuk jalur ini. | Terima `tanggal` dari payload dan validasi. |
| M3 | `inventory/services.py:499-556` + `views.py:242-252` | `GET /api/v1/inventory/verifikasi/` menjalankan tiga fungsi verifikasi sekaligus; `verifikasi_kepemilikan()` melakukan **1 query agregat per baris `Stok`**, `verifikasi_posisi_cache()` **1 per baris `PosisiKlaim`**, `verifikasi_pool_bersih()` mewarisi N+1 dari `isi_pool()` (1 query tarif per produk). | Untuk 500 baris stok = 500+ query dalam satu request, tanpa paginasi dan tanpa batas waktu. Endpoint ini akan time-out begitu data bertambah. | Ganti loop dengan satu agregat `annotate(Sum(...))` + `exclude(qty=F('total'))`. |
| M4 | `produksi/views.py:70` | `SesiInput.objects.filter(...).update(tangki_id=…)` dijalankan **di luar blok atomic mana pun**, sebelum `services.mulai_sesi()` dipanggil `:73`. | Kalau `mulai_sesi()` gagal (stok pool kurang), penugasan tangki tetap tersimpan — sesi DRAFT jadi punya state parsial. | Pindahkan penugasan tangki ke dalam `mulai_sesi()`. |
| M5 | `inventory/services.py:54-60` | `_stok()` memakai `Stok.objects.select_for_update().get_or_create(...)`. Untuk baris yang **belum ada**, `SELECT … FOR UPDATE` tidak mengunci apa pun. | Dua transaksi bisa sama-sama INSERT baris `Stok` pertama untuk kombinasi produk/grup/lapis; `uq_stok_rak`/`uq_stok_tangki` menangkapnya sebagai `IntegrityError` **500**, bukan retry. Sama untuk `SaldoEntitas` (`:120`), `PosisiKlaim` (`:152`), `PeriodeAkuntansi` (`core/services.py:50`). | Tangkap `IntegrityError` dan retry, atau pakai `INSERT … ON CONFLICT DO NOTHING` lalu `SELECT FOR UPDATE`. |
| M6 | `inventory/services.py` (lintas fungsi) | Urutan pengambilan lock tidak seragam: `setor_ke_pool` mengunci RAW-stok → tangki-raw → saldo → POOL-stok → tangki-pool → posisi (`:228-255`); `klaim_hasil` mengunci POOL-stok → tangki-pool → JADI-stok → saldo → posisi (`:334-356`); `terima_barang` mengunci PO → PO-item → stok → saldo (`warehouse/services.py:88-131`). | Dua transaksi paralel pada grup bahan yang sama bisa saling menunggu → **deadlock** yang hanya diselesaikan Postgres dengan membunuh salah satu transaksi (HTTP 500 bagi pengguna). Beban penerimaan pagi hari untuk produk yang sama adalah skenario nyata. | Tetapkan urutan lock kanonik (mis. selalu `Tangki` → `Stok` → `SaldoEntitas` → `PosisiKlaim`, semuanya urut `pk`) dan patuhi di semua fungsi. |
| M7 | `inventory/services.py:229-231` | `setor_ke_pool()` memakai `SaldoEntitas.objects.select_for_update().get(...)` tanpa penanganan `DoesNotExist`. | Entitas yang belum punya saldo untuk stok itu memicu **500**, bukan 400 dengan pesan yang bisa dibaca. | `try/except DoesNotExist → ValidationError`. |
| M8 | 6 lokasi | `.get()` tanpa penjaga → `DoesNotExist` = HTTP 500: `akunting/views.py:172` (`Entitas`), `akunting/services.py:58` (`Entitas`), `:302` (`Suplier`), `:443` (`PurchaseOrder`), `:503` (`PenerimaanBarang`), `warehouse/services.py:307-309` (`PenerimaanBarang`/`PenerimaanItem`), `core/services.py:72` (`PeriodeAkuntansi`). Ditambah `timezone.datetime.fromisoformat()` tanpa penjaga di `akunting/views.py:173` dan `:310`. | Id atau tanggal tak sahih menghasilkan 500 + traceback (bocor kalau `DEBUG=True`), bukan 404/400. | `get_object_or_404` atau `try/except`. |
| M9 | `keuangan/models.py:51-61` + `services.py:22-24` | `RekeningBank` tidak punya `UniqueConstraint(entitas, jenis)`; `catat_pengeluaran()` mengambilnya dengan `.filter(entitas, jenis='KAS_KECIL').first()`. | Dua rekening kas kecil untuk satu entitas → yang dipotong dipilih menurut `ordering` default (`entitas__kode, nama_bank`), bukan menurut niat pengguna. Tidak ada pesan galat. | Tambah unique constraint, atau jadikan rekening parameter eksplisit di payload. |
| M10 | `keuangan/models.py:107-109` vs `services.py:42` | Konvensi tanda tidak konsisten: `MutasiKas.__str__` merender `debit` sebagai `−` dan `kredit` sebagai `+`, tapi satu-satunya penulis (`catat_pengeluaran`) mencatat uang **keluar** di kolom `kredit` sambil **mengurangi** saldo. | Setiap laporan/admin yang membaca `__str__` menampilkan pengeluaran dengan tanda `+`. Penulis berikutnya (pembayaran suplier, penerimaan piutang) akan menebak salah. | Tetapkan konvensi tertulis dan seragamkan; tambah `CheckConstraint` yang mengaitkan `saldo_akhir` dengan arah. |
| M11 | `keuangan/models.py:181-200` | `PengeluaranKas` satu-satunya model transaksional yang **tidak** mewarisi `DiauditModel` — tidak ada `dibuat_oleh`. Parameter `user` di `catat_pengeluaran()` hanya diteruskan ke `posting()`. | Siapa yang mengeluarkan uang tidak tercatat di baris pengeluaran. `pemohon` adalah teks bebas (`:185`), bukan FK — bisa diisi nama siapa saja. Tidak ada `CheckConstraint(nominal > 0)`. | Warisi `DiauditModel`; jadikan `pemohon` FK ke `Profil` atau setidaknya validasi. |
| M12 | `master/views.py:19-22` | `BasisMaster.get_permissions()` mengembalikan `SudahLogin` untuk GET/HEAD/OPTIONS — **tanpa cek modul `master`**. Sama untuk `core/views.py:56` (`PeriodeAkuntansiViewSet` baca). | Setiap pengguna aktif membaca seluruh katalog produk, suplier (termasuk NPWP, kontak, `termin_hari_default`), dan status periode akuntansi. Mungkin disengaja, tapi tidak sejalan dengan `AKSES_MODUL['master'] = [ADMIN]` yang dikirim ke frontend. | Putuskan secara sadar dan dokumentasikan; kalau memang publik-internal, hapus `master` dari `AKSES_MODUL` agar tidak menyesatkan. |
| M13 | `warehouse/services.py:88,124-125,139,174-175,397-400` + `warehouse/views.py:19` + `warehouse/serializers.py:19` | App `warehouse` mengimpor `akunting.models` di tingkat modul dan **menulis langsung** ke `akunting_purchase_order.status` dan `akunting_purchase_order_item.qty_diterima`. | Melanggar aturan lapis 4 yang ditulis `settings.py:87`. Tidak ada satu pintu (`akunting.services`) untuk perubahan status PO, sehingga aturan bisnis PO tersebar di dua app. Perubahan aturan status PO harus dicari di dua tempat. | Sediakan `akunting.services.catat_realisasi_penerimaan(po_id, items)` dan panggil itu. |
| M14 | `keuangan/services.py:7` | `from akunting.services import posting` di tingkat modul, padahal `keuangan/models.py:7-9` menyatakan impor ke akunting **wajib di dalam fungsi**. | Belum menimbulkan siklus (akunting belum mengimpor keuangan), tapi menghapus jaring pengaman: satu impor balik dari `akunting` ke `keuangan` akan membuat Django gagal start. | Pindahkan ke dalam `catat_pengeluaran()`, sesuai aturan yang sudah ditulis sendiri. |
| M15 | `dokumen/urls.py:5-6` + `akunting/services.py:406` | `dokumen` tidak punya endpoint apa pun, dan satu-satunya kode yang membuat `Lampiran` (`lampirkan_dokumen()`) **tidak dipanggil siapa pun**. | Empat model punya FK ke `dokumen.Lampiran` (`FakturPembelian.dokumen`, `PenerimaanBarang.dokumen`, `LaporanSelisih.foto`, `Lampiran.digantikan_oleh`) dan tiga service menerima parameter `dokumen_id`/`foto_id` — tapi **tidak ada jalur unggah**. Semua parameter itu hanya bisa diisi `null`. Instruksi di `warehouse/services.py:76-79` ("unggah dulu, lalu kirim `dokumen_id`") tidak bisa dijalankan. | Bangun endpoint unggah di `dokumen`, atau hapus parameter yang tidak bisa dipakai. |
| M16 | `inventory/urls.py:17-18` · `produksi` · `master/urls.py:10-13` | Master data yang **wajib** untuk operasi tidak punya endpoint tulis: `NilaiEkuivalen` (read-only, tapi `_catat_klaim()` gagal keras tanpanya — `inventory/services.py:142`), `Resep`/`ResepItem` (tidak ada endpoint, tapi `buat_sesi()` gagal tanpanya — `produksi/services.py:108`), `Satuan` (FK wajib di `Produk`, viewset ada tapi tidak dirutekan), `Kategori`, `Pelanggan`. | Sistem tidak bisa dioperasikan dari API saja — Django admin wajib dipakai untuk menyiapkan data. Untuk `NilaiEkuivalen`, artinya seluruh mekanisme klaim SZA bergantung pada data yang hanya bisa diinput lewat admin. | Rutekan `SatuanViewSet`/`PelangganViewSet` (`master/urls.py`), tambahkan viewset untuk `NilaiEkuivalen` dan `Resep`. |
| M17 | `docs/_scan/openapi.yaml` vs kode | Schema OpenAPI mengiklankan **16 operasi tulis yang selalu 405** (PO update/delete, faktur create/update/delete, penerimaan update/delete, laporan-selisih update/delete, sesi update/delete) dan **3 DELETE yang membalas 400** (`profil`, `produk`, `suplier`). Ditambah 11 endpoint tanpa skema body sama sekali (48 error generator). | Klien yang di-generate dari schema akan memanggil operasi yang tidak pernah bekerja; dokumentasi `/api/docs/` menyesatkan konsumen API. | `@extend_schema(exclude=True)` untuk yang 405, dan `serializer_class`/`@extend_schema` untuk 11 APIView tanpa skema. |
| M18 | 8 endpoint | List tanpa paginasi: `profil/menunggu/`, `purchase-order/outstanding/`, `faktur/jatuh-tempo/`, `faktur/aging/`, `laporan-selisih/terbuka/`, `posisi-klaim/`, `work-order/mading/`, `work-order/staff/`. | Semua mengembalikan array penuh. `outstanding`, `jatuh-tempo`, dan `terbuka` tumbuh seiring volume transaksi dan akan menjadi respons multi-MB. | Pakai `self.paginate_queryset()` + `get_paginated_response()`. |
| M19 | `akunting/models/pembelian.py:158` | `PurchaseOrderItem.purchase_order` memakai `on_delete=CASCADE` pada data akuntansi. `PurchaseOrder.delete()` (`:129`) hanya mengizinkan DRAFT — tapi `delete()` model **dilewati** oleh `QuerySet.delete()` (bulk delete di Django admin, atau `PurchaseOrder.objects.filter(...).delete()`). | PO berstatus TERKIRIM tanpa penerimaan bisa dihapus beserta seluruh itemnya lewat admin. (PO yang sudah ada penerimaan terlindungi oleh `PenerimaanItem.po_item` yang `PROTECT`.) | `on_delete=PROTECT` + `has_delete_permission = False` di `PurchaseOrderAdmin`. |
| M20 | `work_order/models.py:56` | `WorkOrderPenugasan.staff` memakai `on_delete=CASCADE` ke `Profil`, berlawanan dengan kebijakan yang ditulis `staff_user/models.py:22-25` ("semua model transaksional memakai PROTECT ke user"). | `Profil.delete()` memang raise, tapi `Profil.objects.filter(...).delete()` melewatinya dan menghapus penugasan. | `PROTECT`. |
| M21 | `warehouse/services.py:231-269` | `_periksa_selisih()` tidak pernah menerbitkan `KURANG_KIRIM`/`LEBIH_KIRIM`; property `PenerimaanItem.selisih_po` (`warehouse/models.py:184`) yang dirancang untuk itu tidak dipakai siapa pun. Pengiriman **curah** (`qty_deklarasi = 0`) melewati seluruh pemeriksaan berat. | Kurang kirim koli hanya terdeteksi kalau kebetulan juga menimbulkan selisih berat. Untuk pengiriman curah **tidak ada kontrol berat sama sekali**. | Tambahkan cabang ketiga berbasis `selisih_po`, dan untuk curah bandingkan `qty_diterima` terhadap `po_item.sisa_qty`. |
| M22 | `akunting/models/hutang.py:274-277` + `services.py:228` | `UangMukaSuplier.sisa` diisi saat pembuatan dan **tidak pernah dikurangi kode mana pun**. | Uang muka masuk tapi tidak pernah dialokasikan ke faktur berikutnya. Saldo uang muka tumbuh selamanya. | Bangun alokasi uang muka di `alokasi_pembayaran()` (pakai uang muka dulu sebelum kas). |
| M23 | `warehouse/services.py:403-419` | `tutup_laporan()` tidak memeriksa apakah laporan sudah `DITUTUP` (hanya menolak `DISELESAIKAN`, `:407`). | Bisa dipanggil berulang; `diselesaikan_pada`/`diselesaikan_oleh`/`catatan_resolusi` ditimpa setiap kali → jejak audit hilang. Status `DISEPAKATI` (`models.py:230`) tidak punya transisi menuju maupun keluar — kode mati di mesin status. | Tolak kalau status sudah final; hapus atau sambungkan `DISEPAKATI`. |
| M24 | `core/services.py:79` | `buka_periode()` meng-append jejak ke `alasan_buka` (TextField) setiap kali dipanggil, tanpa batas. | Buka-tutup berulang menggelembungkan satu kolom teks; tidak bisa di-query per kejadian. Ini persis yang model `audit.JejakAktivitas` dirancang untuk tangani (lihat H11). | Catat ke tabel jejak, bukan ke kolom teks. |

---

## LOW

| # | Lokasi | Masalah | Dampak | Saran perbaikan |
|---|---|---|---|---|
| L1 | seluruh repo | **Tidak ada satu pun test.** 14 berkas `tests.py` semuanya scaffold kosong. | Tidak ada jaring pengaman untuk 17 algoritma di FASE 3, termasuk jalur uang dan invariant SZA. | Mulai dari test invariant: posting seimbang, idempotensi, `SUM(SaldoEntitas)=Stok.qty`. |
| L2 | `produksi/serializers.py:11` | `satuan_kode` memakai `source='resep.produk_jadi.satuan_kode'`; `master.Produk` tidak punya atribut itu (`master/models.py:106` hanya FK `satuan`). | Karena `read_only=True` → `required=False`, DRF melempar `SkipField` dan **field hilang diam-diam dari respons** (bukan 500). Frontend menerima objek tanpa `satuan_kode`. | `source='resep.produk_jadi.satuan.kode'`. |
| L3 | `produksi/views.py:23-26` | `get_serializer_class()` mengembalikan `SesiListSerializer` di kedua cabang. | Percabangan mati; menyesatkan pembaca. | Hapus percabangan atau buat serializer detail. |
| L4 | `keuangan/views.py:49` | `total_pemasukan` di-hardcode `0`. | Dashboard menampilkan angka palsu. | Hitung dari `MutasiKas.debit`, atau hilangkan field-nya. |
| L5 | `keuangan/views.py:35-37` | `except Exception` telanjang mengubah `TypeError`, `DoesNotExist`, `IntegrityError` menjadi HTTP 400 dengan pesan Python mentah. | Galat pemrograman tersamar sebagai galat validasi; pesan internal bocor ke klien. | Tangkap `ValidationError` saja, biarkan sisanya jadi 500 yang tercatat di log. |
| L6 | `master/urls.py:4` dan `:6` | `ProdukViewSet` diimpor dua kali (absolut lalu relatif). | Tidak berbahaya, tapi menandakan penyuntingan yang tidak dibersihkan. | Hapus baris `:4`. |
| L7 | 6 berkas | Fungsi `_galat(e)` identik disalin di `core/views.py:23`, `staff_user/views.py:26`, `inventory/views.py:39`, `warehouse/views.py:33`, `akunting/views.py:35`; versi `produksi/views.py:11` **berbeda** (memakai `e.message`, bukan `e.message_dict`) sehingga galat dict di produksi tampil sebagai `str(dict)`. | Format galat tidak seragam antar modul; frontend harus menangani dua bentuk. | Pindahkan ke `core/exceptions.py` sebagai exception handler DRF (`EXCEPTION_HANDLER`). |
| L8 | 21 berkas | Berkas kosong 0 byte hasil `scaffold.ps1`: 9× `permissions.py`, 5× `services.py`, 3× `serializers.py`, dan 4× `views.py` scaffold. `akunting/permissions.py` justru **tidak ada** meski app lain punya. | Kebisingan; menyulitkan pembaca membedakan "belum dibuat" dari "sengaja kosong". | Hapus yang kosong; buat `.gitkeep` kalau strukturnya memang mau dipertahankan. |
| L9 | 10 berkas | BOM UTF-8 di awal berkas (`core/constants.py`, `core/exceptions.py`, `core/urls.py`, `dokumen/urls.py`, `keuangan/urls.py`, `logistik/urls.py`, `pajak/urls.py`, `produksi/urls.py`, `sales_order/urls.py`, `work_order/urls.py`). | Python menanganinya, tapi alat statis yang membaca `encoding='utf-8'` polos gagal parse. | Simpan ulang tanpa BOM. |
| L10 | `akunting/models/pembelian.py:202`, `warehouse/models.py:72-73` | Indeks berlebihan: `ix_poitem_po` (FK sudah berindeks), `ix_penerimaan_tanggal` dan `ix_penerimaan_selisih` (kolomnya sudah `db_index=True`). | Tiga indeks ganda — biaya tulis dan ruang tanpa manfaat baca. | Hapus salah satu dari tiap pasangan. |
| L11 | `master/models.py:151`, `keuangan/models.py:186`, `akunting/models/pembelian.py:220`, `warehouse/models.py:198` | Angka literal `18,2` / `Decimal('0.01')` alih-alih konstanta di `core/constants.py`, melanggar aturan yang ditulis `core/constants.py:1`. | Perubahan presisi harus dicari manual. | Pakai konstanta. |
| L12 | `core/models.py:137` | `CounterDokumen.jenis` `CharField(16)` **tanpa `choices`**; daftar nilai hanya ada di komentar. | Salah ketik menciptakan seri counter baru diam-diam (`PO` vs `P0`) dan nomor mulai dari 001 lagi. | `TextChoices`. |
| L13 | `produksi/models.py:49-51` · `warehouse/models.py:184` · `staff_user/permissions.py:167,182` · `staff_user/serializers.py:15` · `akunting/models/pembelian.py:134-153` · `akunting/posting_rules.py:60-90` · `akunting/models/akun.py:89` · `keuangan/models.py:128` · `inventory/services.py:364,559` | Kode mati terverifikasi: `Resep.susut_wajar` (tidak pernah dibaca), `PenerimaanItem.selisih_po`, permission `AksesEntitas` & `BacaSaja` (tidak dipasang di view mana pun), `ProfilRingkasSerializer`, `PurchaseOrder.kirim()`/`.batalkan()` (duplikat service), 5 aturan posting tanpa pemanggil, model `SaldoAkunBulanan` & `RencanaBayar` (tanpa penulis), `lunasi_posisi()`, `verifikasi_rantai_saldo()`. | Pembaca mengira fitur itu ada. `SaldoAkunBulanan` khususnya menjanjikan checkpoint bulanan yang membuat laporan cepat — dan tidak ada. | Hapus, atau tandai `# TODO` dengan alasan. |
| L14 | `staff_user/authentication.py:46` | Umur token dihitung dari `token.created` yang **tidak pernah diperbarui**. | Sesi mati keras setelah 12 jam meski pengguna aktif. Login baru menghapus token lama (`services.py:227`) → satu sesi per pengguna, perangkat kedua melempar yang pertama. | Sliding expiry, atau jelaskan sebagai keputusan sadar. |
| L15 | `SPECTACULAR_SETTINGS` `settings.py:174-178` | Generator melaporkan tabrakan nama enum (`Status5faEnum`, `StatusD25Enum`) karena beberapa model punya field `status` dengan `choices` berbeda. | Tipe di client yang di-generate bernama tidak bermakna. | Isi `ENUM_NAME_OVERRIDES`. |

---

## Hal yang sudah BENAR (agar tidak dirusak saat perbaikan)

Bagian ini bukan temuan — dicatat supaya perbaikan di atas tidak menghilangkan
kekuatan yang sudah ada.

| Praktik | Lokasi |
|---|---|
| **CONSTRAINT TRIGGER DEFERRABLE** untuk keseimbangan jurnal — satu-satunya penjaga yang tidak bisa dilewati `bulk_create`/shell/migrasi | `akunting/migrations/0004_trigger_jurnal_seimbang.py:46-49` |
| Penomoran dokumen dengan `select_for_update` + kolom counter (bukan `count()+1`) | `core/models.py:181-201` |
| **Pemisahan serializer gudang vs akunting berbasis kelas**, bukan `pop()` — field harga memang tidak ada di kelasnya | `warehouse/serializers.py:4-15`, `inventory/serializers.py:4-13` |
| Nilai rupiah selalu dihitung **di server** dari harga PO; payload gudang tidak pernah membawa angka uang | `warehouse/services.py:190`, `:281-282` |
| `idempotency_key` `UNIQUE` di `MutasiStok`, `MutasiKlaim`, `MutasiKas`, `JurnalUmum` — infrastrukturnya benar (yang salah hanya dua pemanggil, C3/C4) | `inventory/models.py:165,316`, `keuangan/models.py:89`, `akunting/models/jurnal.py:64` |
| `order_by('tanggal_jatuh_tempo','id')` deterministik sebelum `select_for_update` untuk mencegah siklus lock | `akunting/services.py:212` |
| Idempotensi berbasis kunci deterministik yang benar di produksi | `produksi/services.py:156,197` |
| `AksesModul` **gagal tertutup** — view tanpa atribut `modul` ditolak, bukan dibuka | `staff_user/permissions.py:109-112` |
| Aturan akses sebagai **data** (satu dict), bukan `if` yang tersebar | `staff_user/permissions.py:24-41` |
| Akun tidak pernah dihapus; 80 dari 84 relasi memakai `PROTECT` | `staff_user/models.py:235`, seluruh model |
| Append-only ditegakkan di `save()`/`delete()` untuk `MutasiStok`, `MutasiKlaim`, `MutasiKas`, `KartuHutang`, `RiwayatAkses`, `JurnalUmum`, `JurnalDetail`, `Lampiran` | 8 model |
| Semua nilai uang `DecimalField` — **0 `FloatField`** di seluruh repo | grep terverifikasi |
| `TIME_ZONE = 'Asia/Jakarta'` dengan alasan tertulis (transaksi sebelum 07:00 WIB) | `settings.py:219-222` |
| `.env` **tidak terlacak git**; `.gitignore` menutup `.env`, `media/`, `logs/`, `staticfiles/` | `.gitignore:1-3` |
| Blok keamanan produksi lengkap saat `DEBUG=False` (HSTS, SSL redirect, cookie secure, nosniff, X-Frame-Options) | `settings.py:258-267` |
| Path Django admin diobfuskasi | `pracindo_erp/urls.py:9` |
| Pemecahan migrasi 3-tahap yang benar untuk memutus siklus `akunting ↔ warehouse` | `akunting/migrations/0001-0003` |
| `aging_hutang()` — satu query agregat, bukan loop per suplier | `akunting/services.py:660-674` |
| `hitung_kapasitas()` — 2 query, tanpa N+1, `ROUND_DOWN` yang benar | `produksi/services.py:53-70` |
| Model dan migrasi **sinkron**: `makemigrations --check --dry-run` → "No changes detected" | terverifikasi |
